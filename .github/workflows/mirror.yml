# [SUSPENDED 2026-07-19 · 사유 정정 2026-09-08] Daily data mirror — schedule is off.
#
# ★2026-07-19 에 적힌 사유는 **더 이상 사실이 아니다**: "한국투자증권 OpenAPI 가
#   시세의 제3자 재배포를 허용하지 않으므로 검토를 마치기 전엔 켜지 마라."
#   2026-07-22 에 공공데이터로 전환했다. 원천은 금융감독원 DART·금융위원회
#   공공데이터포털이고, 출처를 표기하면 영리 목적을 포함해 재배포할 수 있다.
#   그 검토는 끝났다 — **라이선스는 더 이상 이 워크플로를 막는 이유가 아니다.**
#
# ★그렇다고 자동으로 켜지 않는다. 지금 데이터는 사이트가 매 거래일 직접 발행하고
#   (https://aikstockdata.com/data/public/index.json), 깃 저장소에 같은 파일을 또
#   쌓을 이유가 있는지는 **결정할 문제**이지 기술적 제약이 아니다. 켜려면 그
#   결정을 먼저 하고, 무엇을 얼마나 보관할지 정한 뒤에 켠다.
#
# 이 저장소의 실제 쓸모는 watchdog.yml 이다(사이트 가용성·신선도 감시).

name: Daily data mirror (suspended)

on:
  workflow_dispatch:

jobs:
  suspended:
    runs-on: ubuntu-latest
    steps:
      - name: Suspended notice
        run: |
          echo "Mirroring is suspended for license compliance review (2026-07-19)."
          echo "No data is fetched or committed."
