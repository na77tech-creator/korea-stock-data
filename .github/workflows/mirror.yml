# [SUSPENDED 2026-07-19] Daily data mirror — suspended for license compliance review.
# The upstream source (Korea Investment & Securities OpenAPI) does not permit
# third-party redistribution of market quote data. Mirroring is stopped until
# the site's public data layer is rebuilt with licensed/derived indicators only.
# Do NOT re-enable the schedule without completing that review.

name: Daily data mirror (suspended)

on:
  workflow_dispatch:

jobs:
  suspended:
    runs-on: ubuntu-latest
    steps:
      - name: Suspended notice
        run: |
          echo "Mirroring is suspended for license compliance review (2026-07-19)."
          echo "No data is fetched or committed."
